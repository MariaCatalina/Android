<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/holo_orange_dark"
    android:gravity="center"
    >

    <Button
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:id="@+id/showImages"
        android:text="show images"
        android:onClick="showImages"

        />


    <Button
        android:layout_below="@id/showImages"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:id="@+id/showTabs"
        android:text="show tabs"
        android:onClick="showTabs"
        />

    <Button
        android:layout_below="@id/showTabs"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:id="@+id/showTabsWithTextAndIcon"
        android:text="show tabs with text and icon"
        android:onClick="showTabsWithTextAndIcon"
        />


</LinearLayout>